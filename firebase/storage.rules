rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/{allPaths=**} {
      // Simplificado: apenas usuários logados podem ler/gravar arquivos
      allow read, write: if request.auth != null;
    }
  }
}
